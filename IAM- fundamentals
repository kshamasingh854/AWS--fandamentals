* IAM (Identity and access management system (IAM):=

* is a web service that helps you securely control access to AWS resources. you use IAm to control who is authenticated (signed-in) 
and authorized(has permission) to use Resources.
 
*authenticated - user, pass
authorize:- jiski permission h uske pass

* limites of aws resources:-
300 group per aws account
* 1000 IAm roles per Aws acoount
* manage police attached an IAm role and IAm user is 10
 
1) Shared account to your AWs account
2) granular permission
3)Secure access to aws resourecs for application that run on amazonEC2
4) multifactor authentication
5) identity federation 



*IAM TERMS and components:--
1) Principal - user, application, fadrating user (jo rsources ko use krra h)
2) Request - resource request by user
3) Authentication - user, passwd, only for authenticusrer
4)Authorization- which permission you have, to use resouces.
5) Action/oprations (user anything is doing in aws that is action)
6) Resources. :- AWs all services are resources.

*Principle:-- IAM users is your first principle.
* Actions:- define by a service and are the things that you can do to a resource such as viewing, 
creating, editing and deleting that Resource.

*Identity fadration:-- If your account user already have a way to be authenticated
such as authentication through your corporate netwwork

*IAM IDENTITIES:-
* USERS
* GROUP :- COLLECTIOS OF USERS
* ROLE :- identity with permission policy that determine what the identity can and con not do in aws
*policy:-you can assign access policy to users which permission you will give
*inline policy:- it will show more options to give specific permission for users.
* Role :-- use for fadrated user


* PERMISSION NA POLICE:--
identity based POLICE:-- set the permission are granted through police to user, role, group  

*By default,IAM can not access anythingin in your account
*user police:- that define the effect, actions, resources and optional conditions.
*Resource based police:- at a time user can accesss roles and resource. both. 
user at time can use only one role. 


*IAM USER - The Root USEr:- that has all permission to don any thuing in aws account.

*An IAm user with addministrator permissions is not the same things as the AWS account root user.

*trusted account - jha user created h 
trusting account - jhs resources allow hai

*Cross account Permission.:--
you can define a role in the trusting account that specifes what permissions the IAm users in the other 
account are allowed.

* MFA - extra layer of security.


