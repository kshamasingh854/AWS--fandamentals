* ELB ( ELASTIC LOAD BALANCER) (region specific)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                

*Load balancer:-- Ft (fault taolrance, high valablity(HA)
:- load balancer distribute the web traffic to the valable servers


types of load balancer:
1) Application load balancer  (only work on http and https traffic)
2) Network load balancer / TCP/UDP/TLS
3) classic Load balancer  (PRIVIOUS Generation)

*target group - group of ec2
target - ec2

* by default, the ELB distributes traffic evenly between the AZ it is defind in without condideration to the number of registration 
EC2 instancesin each AZ
* by default, ELB distribute the trrafic bassed on AZ, not based on EC2 instamce 

* CROSS ZONE LOAD BALANCING:--
*Disable by default.

* when enabled, the ELB will distribute traffic evenly between registred EC2 instances.
it's region specifid.
* only one subnet can be define for the ELB in an AZ.
* ELB should always be accessed using DNS not IP
*ELB nodes will have public ip address.

*Target group:--
 
*Application LB LAB:--
We will create two instance in diffret AZ and create html file in nstance.
*create Application ELB and add both AZ, and create target group add both ec2 instance 
we will access the web page using ELB DNS from webbrowser.

*Network LB LAB:--
*create two ec2 instamces in diffrent AZ create html file.
*create Netwok LB and add both AZ, craete target group and add private ip of both instnces.
and access the we page from browser using DNS Of Loadbalancer.

